<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android"
    android:enterFadeDuration="@android:integer/config_mediumAnimTime"
    android:exitFadeDuration="@android:integer/config_mediumAnimTime">
    <!-- 按壓時 -->
    <item android:state_pressed="true">
        <!--rectangle 長方形 可以畫出直角形，圓角形，弧形等-->
        <shape android:shape="rectangle">
            <!--設定圓角，當設置的圓角半徑很大，就可變成弧形邊了-->
            <corners android:radius="15dip" />
            <!--邊框-->
            <stroke android:width="0.5dip" android:color="#FF9800" />
            <!--漸層顏色-->
            <gradient android:angle="-90" android:startColor="#FFF4D5" android:endColor="#D6A606"  />
        </shape>
    </item>

    <!-- 預設時 -->
    <item android:color="@android:color/black">
        <shape android:shape="rectangle"  >
            <corners android:radius="10dip" />
            <stroke android:width="0.5dip" android:color="#000000" />
            <gradient android:angle="-90" android:startColor="#FFC107" android:endColor="#FFFFFF"  />
        </shape>
    </item>

</selector>